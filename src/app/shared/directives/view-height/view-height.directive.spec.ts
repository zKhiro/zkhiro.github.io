import { ViewHeightDirective } from './view-height.directive';

describe('ViewHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewHeightDirective();
    expect(directive).toBeTruthy();
  });
});
