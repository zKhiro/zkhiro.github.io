import { SkillHandlerDirective } from './skill-handler.directive';

describe('SkillHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
