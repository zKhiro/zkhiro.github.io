import { PositionStickyDirective } from './position-sticky.directive';

describe('PositionStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new PositionStickyDirective();
    expect(directive).toBeTruthy();
  });
});
